{
  "name": "firefox-ext-legacydeck",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "zip": "rm -f dist.zip && cd public && zip ../dist.zip $(find . -type f)"
  },
  "keywords": [],
  "author": "",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "typescript": "^5.2.2",
    "web-ext-types": "^3.2.1"
  }
}
